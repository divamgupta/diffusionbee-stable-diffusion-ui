root = true

# Window-style newlines with a newline ending every file
[*]
end_of_line = crlf
insert_final_newline = true
trim_trailing_whitespace = true

# 4 space indents on anything not in ./backends/
[!backends/**]
indent_style = space
indent_size = 4
